# YGO 项目协作规范

本文件适用于仓库根目录及其全部子目录。参与本项目开发的人员与自动化代理都必须遵守以下规则。

## 项目目录边界

- 项目源码、脚本、配置、文档、第三方依赖、测试、构建脚本和构建产物必须保存在 `/Volumes/WD/YGO` 内。
- 不得在主盘或仓库外创建项目代码副本、项目备份、依赖副本或长期保留的项目文件。
- 编译器缓存、Godot 编辑器缓存、系统临时文件以及 Codex/Godot MCP 的全局配置不属于项目文件，可以由对应工具保存在其默认位置。
- 确需在仓库外创建临时文件时，只能使用系统临时目录，并应在任务结束后清理；不得把临时目录当作项目存储位置。
- 创建备份前必须说明备份用途、位置、预计大小和清理方式，并取得用户同意。

## 注释要求

- 新增或修改的自有代码必须包含充分、准确的中文注释，使后续开发者无需猜测即可理解关键意图。
- 对外接口、核心类、复杂数据结构、规则映射、资源生命周期、线程或内存所有权、异常路径以及不直观的算法必须详细说明。
- 注释重点解释“为什么这样设计”“输入输出约束是什么”“失败时如何处理”，不得只把代码逐字翻译成自然语言。
- 涉及 OCGCore、Godot GDExtension、卡片数据格式或 Lua 脚本协议时，应注明双方字段或生命周期的对应关系。
- 简单赋值、显而易见的控制流和惯用代码不需要机械地逐行注释，避免注释噪声。
- 修改行为时必须同步更新相关注释；过期或与实现矛盾的注释视为缺陷。
- 不得为了满足注释要求而修改第三方子模块、自动生成文件或构建缓存中的代码。

## 中文输出要求

- 项目自有代码产生的打印、日志、警告、错误、断言说明和用户可见诊断信息必须使用简体中文。
- 命令行工具、测试程序和 Godot 界面中的状态说明也必须使用简体中文。
- 第三方库原样输出、系统错误文本、协议规定的固定字符串以及无法控制的工具输出可以保留原文；项目在转述这些信息时应补充中文上下文。
- 日志应包含定位问题所需的信息，但不得输出密码、令牌、隐私数据或大段无关内容。

## Godot 原生能力与 C++ 职责边界

- OCGCore、卡片数据库、规则消息解析、响应编码、决斗状态持久化和不可信输入校验属于 C++/GDExtension 层。该层必须提供经过验证的语义接口，不得要求 Godot 界面拼装 OCGCore 原始字节响应或自行推断规则结果。
- 战场、卡位、手牌、卡片详情、生命值面板、阶段控件、确认浮层和系统工具等可视结构应优先制作成 Godot `.tscn` 场景，并按职责拆成可复用子场景；不得把整套稳定界面长期保留为 GDScript 中的 `Node.new()` 动态拼装代码。
- 字体、颜色、间距、圆角、控件状态和黑白原型样式应优先放入 `.tres` Theme、StyleBox、Font 或其他资源；不得在多个脚本中重复硬编码同一套视觉参数。
- 自适应布局应优先使用 `Control` 锚点、Container、Size Flags、Stretch、最小尺寸和安全边距。禁止只针对开发机分辨率写死绝对坐标；原生全屏、1080p、4K 和常见宽高比必须共享同一套布局逻辑。
- 卡牌移动、选中反馈、攻击指示、目标高亮、阶段切换和数值变化应优先使用 Godot 的 AnimationPlayer、Tween、Shader、粒子、音频与场景生命周期能力。GDScript 只负责状态绑定、轻量表现编排和信号转发，不应重新实现引擎已有的布局、动画或资源系统。
- Godot 层只能展示 C++ 暴露的公开决斗快照和合法候选动作。用户点击卡牌、区域或玩家面板后，Godot 应提交候选索引或稳定语义参数，再以 OCGCore 返回的新快照刷新画面；界面不得先行修改生命值、区域归属、胜负或卡牌位置来伪造成功。
- C++ 与 Godot 的边界数据必须使用含义明确、可测试的 Dictionary、Array 或绑定值对象。新增字段时应同时说明 OCGCore 来源、Godot 消费方、缺省值和终局/错误语义，并补齐原生测试与 Godot 交互测试。
- 只有确实需要运行时生成、数量不固定或由规则数据驱动的节点才应动态创建；即使动态创建，也应优先实例化预制 `.tscn` 子场景，而不是在业务脚本中逐项设置全部视觉属性。

## Godot MCP 使用与引擎验收

- 涉及场景、布局、主题、动画、输入或运行时交互的任务必须使用 Godot MCP 参与实现或验收。不得只修改文本文件并以静态代码审阅代替引擎内验证。
- 创建或修改场景时，应优先使用 Godot MCP 的场景、节点、资源、主题、信号和动画相关能力，并通过 MCP 读取实际场景树与关键属性，确认编辑结果已被 Godot 正确加载。
- 运行时验收必须通过 Godot MCP 启动项目并操作真实节点或输入：至少覆盖本次修改的主路径、一个取消或非法路径，以及状态回传后的界面变化。涉及响应式布局时，还必须检查至少 1920×1080 和 3840×2160，必要时补充窄屏或不同宽高比。
- Godot MCP 验收应关注引擎特性是否真正生效，例如 Container 是否完成重排、Theme 是否被继承、AnimationPlayer/Tween 是否播放、Shader 参数是否更新、信号是否连接，以及 GDExtension 返回状态是否反映到场景。
- MCP 自动注入的临时 Autoload、交互服务器脚本或其他调试文件不属于项目功能。验收结束后必须停止运行实例，检查 `project.godot` 和工作区，清除注入内容，并确认没有把 MCP 临时文件加入提交。
- Godot MCP 不能替代自动化测试。每项功能仍须按风险补充 C++ 单元/集成测试、GDScript 无界面测试或场景契约测试，并在提交前同时运行测试和 MCP 实机验收。
- 若 Godot MCP 当前缺少某种编辑能力，可以用项目脚本或文本方式完成受控修改，但必须说明原因，随后仍要用 Godot MCP 加载、检查并运行该结果；不得因为工具限制退回“全部用代码模拟引擎”的实现方式。

## Git 提交规范

- Git 提交标题和正文必须使用简体中文，并清楚描述本次提交的目的、范围和验证结果。
- 提交标题采用 Conventional Commits 结构：

  ```text
  <类型>(<可选范围>): <中文摘要>
  ```

- 常用类型包括：
  - `feat`：新增功能。
  - `fix`：修复缺陷。
  - `test`：新增或调整测试。
  - `docs`：文档变更。
  - `refactor`：不改变外部行为的重构。
  - `build`：构建系统或依赖变更。
  - `chore`：其他维护工作。
- 标题应简洁、具体，避免“更新代码”“修改文件”等无法说明目的的描述。
- 除极小且含义完整的文档修正外，提交必须包含详细正文，至少说明：
  - 为什么需要这次变更。
  - 实际做了哪些关键修改。
  - 使用什么命令或方式完成验证。
- 一个提交只处理一个清晰主题；不得混入无关格式化、临时文件或其他任务的改动。
- 提交前必须检查工作区、审阅暂存差异，并运行与改动风险相匹配的构建和测试。

## 适用范围与例外

- 本规范约束项目自有内容，不追溯修改已经固定的第三方源码历史。
- `third_party/` 中的外部仓库、自动生成的绑定代码和外部工具自身的日志不强制符合中文注释与中文输出要求。
- 如果某项外部协议要求使用固定英文标识符、文件名或消息文本，应遵循协议，并在项目自有层提供中文说明。
